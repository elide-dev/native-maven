# nmvn variants this example cannot build on, one per line ('#' starts a comment).
# 'all' means every variant. test-nmvn-examples.sh and benchmark-nmvn-examples.sh
# skip this project when run with a listed --variant.
#
# non-crema: maven-war-plugin fails on the Servlet 3.0 probe without runtime
# class loading — diagnosis and options in non-crema-war-servlet-probe.md.
non-crema